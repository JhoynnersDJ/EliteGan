import { Router } from "express";
import {AuditoriaController} from "../controllers/AuditoriaController.js"

const router = Router();

//router.get('/:id',AuditoriaController.index)

export default router;